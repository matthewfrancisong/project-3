<div class = container>
<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :form_type, :value => 'wedding' %>

  <div class="field">
  <div class="input-field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

   <div class="input-field">
    <%= f.label :password %> <i>leave blank if you don't want to change it</i><br />
    <%= f.password_field :password, autocomplete: "on" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> characters minimum</em>
    <% end %>
  </div>

  <div class="input-field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="input-field">
    <%= f.label :current_password %> <i>we need your current password to confirm your changes</i><br />
    <%= f.password_field :current_password, autocomplete: "off" %>
  </div>

  <div class="input-field">
    <%= f.label :Wedding_Date %><br />
    <%= f.date_field :date,  autofocus: true, class: 'datepicker' %>
  </div>

  <div class="input-field">
    <%= f.label :Wedding_start_time %><br />
    <%= f.time_field :time,  autofocus: true, class: 'timepicker' %>
  </div>

  <div class="input-field">
    <%= f.label :Couple_names %><br />
    <%= f.text_field :title,  autofocus: true %>
  </div>

  <div class="input-field">
    <%= f.label :Venue %><br />
    <%= f.text_field :venue,  autofocus: true %>
  </div>

  <div class="input-field">
    <%= f.label :Number_of_tables %><br />
    <%= f.number_field :num_tables,  autofocus: true %>
  </div>

  <div class="input-field">
    <%= f.label :Number_of_seats %><br />
    <%= f.number_field :num_seats,  autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Update", class: 'btn hoverable waves-effect'%>
  </div>
<% end %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: 'btn hoverable waves-effect' %></p>

<%= link_to "Back", :back, class: 'btn hoverable waves-effect' %>

</div>
